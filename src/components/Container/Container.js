import React from 'react'
import { View, SafeAreaView } from 'react-native'
import { colors } from '../../export'

export default function Container() {
    return (
        <SafeAreaView style={{ flex: 1, backgroundColor: colors.BlueJaja }}>
        </SafeAreaView>
    )
}
